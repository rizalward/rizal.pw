# rizal.pw

Public static site for **ЯBOT**:

- **`/`** — installable offline-first **Mind** PWA. Records conversations and notes into a local IndexedDB Mind tape (NEVER-DELETE; corrections amend via new entries). Export/import `.jsonl` for clay ingest. **No network calls from the app page.**
- **`/trueblast/`** — TRUEBLAST portal (dry-run until Decider arms) + [`/trueblast/log.json`](./trueblast/log.json).

Open twin facts: [rizal.info/open.html](https://rizal.info/open.html).

## Publish (when Decider ready)

GitHub Pages from repo `rizalward/rizal.pw`, apex `rizal.pw`. See `/workspace/rizal-sites/DNS-AND-HOSTING.md`.

## License

MIT — see [LICENSE](./LICENSE).
